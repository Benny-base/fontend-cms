import styles from './index.less';
import { Button } from 'antd'

const Page = () => {
   return (
        <div>
            <div className={styles.title}>role index</div>
        </div>
   );
}

Page.label = 'roleManage'

export default Page